package main

import (
	"fmt"
	"testing"
)

func TestExample(t *testing.T) {
	tests := []struct {
		input    int
		expected int
	}{
		{1, 1},
	}

	for _, test := range tests {
		fmt.Println(test)
	}
}
